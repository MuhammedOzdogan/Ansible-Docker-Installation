---
droplets:
  hosts:
    droplet1:
      ansible_host: droplet # This can be ip address or the hostname defined in /etc/hosts file
      ansible_user: sem
      ansible_ssh_private_key_file: ~/.ssh/sem-ecdsa
      ansible_sudo_pass: "{{my_cluster_sudo_pass}}"
